/** Metadata key cho `@Public()` — đọc trong `JwtAuthGuard`. */
export const IS_PUBLIC_KEY = 'isPublic';
/** Metadata key cho `@Roles()` — đọc trong `RolesGuard`. */
export const ROLES_KEY = 'roles';
